[project]
name = "brickhigh-catalog"
version = "0.1.0"
requires-python = ">=3.11"
description = "BrickHigh 開發端目錄與資產驗證工具"

[tool.pytest.ini_options]
testpaths = ["tools/catalog/tests", "tools/acceptance/tests"]
pythonpath = ["."]
addopts = "-ra"

